export default function SettingsPage() {
  return (
    <div>
      <h1 style={{ marginBottom: '1rem', fontWeight: 800, fontSize: '2rem' }}>Settings</h1>
      <p style={{ color: 'var(--text-secondary)' }}>
        Configure client company tenant details, manage role-based permission gates, view audit trails, and subscription tiers.
      </p>
    </div>
  );
}
