export default function ClientsPage() {
  return (
    <div>
      <h1 style={{ marginBottom: '1rem', fontWeight: 800, fontSize: '2rem' }}>CRM Retainers</h1>
      <p style={{ color: 'var(--text-secondary)' }}>
        Track client pipelines, retainer hour usage, renewal alerts, and historical interaction logs.
      </p>
    </div>
  );
}
